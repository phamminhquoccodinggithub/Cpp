#include <iostream>
#include <thread>
#include <algorithm>
#include <list>
using namespace std;
list<int &> mylist;
void addToList(int max, int interval)
{
    for (int i = 0; i < max; i++)
    {
        if ((i % internal) == 0)
        {
            mylist.push_back(i);
        }
    }
}